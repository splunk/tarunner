change_type: breaking

component: featuregates

note: "rename the `cook` feature gate to `enableDataCooking`"

issues: [128]

subtext: |
  The feature gate that enables index-time data cooking is now `enableDataCooking`,
  aligned with the `enableTARunner` gate in splunk-otel-collector. Enable it with
  `--feature-gates +enableDataCooking`.
